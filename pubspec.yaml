name: local_llm_chat
description: Offline local LLM Flutter app

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  http: ^1.2.0
  flutter_markdown: ^0.7.7+1
  just_audio: ^0.9.36
  google_fonts: ^8.1.0
  flutter_animate: ^4.5.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
